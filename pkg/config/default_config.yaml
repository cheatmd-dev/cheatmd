# CheatMD configuration file
# Place at ~/.config/cheatmd/cheatmd.yaml

# Default cheat directory (supports ~ expansion).
# First-run setup installs starter cheat packs here.
path: ~/.local/share/cheatmd/cheats

# Registry of installable cheat packs (used by first-run setup).
# Point this at a private/self-hosted registry.yaml to customize the packs
# offered on setup.
registry_url: "https://raw.githubusercontent.com/cheatmd-dev/registry/main/registry.yaml"

# Output mode: print, copy, exec
output: print

# Shell to use for variable generation
shell: /bin/bash

# Keybindings
# key_widget: Shell widget activation key (bash format: \C-g for Ctrl+G, \C-x for Ctrl+X)
# key_open: TUI key for opening markdown in editor (e.g., ctrl+o, ctrl+e)
key_widget: "\\C-g"
key_open: "ctrl+o"
# key_substitute: opens a fuzzy search of env vars + shell history during
# variable resolution, lets you pick a value to substitute into the prompt.
key_substitute: "ctrl+t"
# key_preview: opens a full-screen markdown preview of the current cheat's
# source file, scrolled to the cheat's section. Esc / q closes.
key_preview: "ctrl+y"
# key_history: opens the execution history picker. Enter on an entry re-opens
# the original cheat with its previous variable values pre-filled.
key_history: "ctrl+h"

# Execution history. Each cheat run appends a line to history_file as JSONL.
#   history_file - path override; empty means $XDG_DATA_HOME/cheatmd/history.jsonl
#                  (or ~/.local/share/cheatmd/history.jsonl as a fallback).
#   history_max  - max entries shown in the picker; 0 = unlimited.
history_file: ""
history_max: 1000

# Substitute search sources. Valid entries: "env", "history".
# Empty list disables the feature.
substitute_sources: ["env", "history"]

# Display options
# Control what appears in the title/list path column
show_folder: true
show_file: true

# Preview section height (number of lines)
preview_height: 6

# UI colors (256-color palette or hex)
# See: https://www.ditig.com/256-colors-cheat-sheet
#color_header: "#cad2e2"     # htb heading for headers
#color_command: "#9fef00"    # htb lime for commands
#color_desc: "#a4b1cd"       # htb text color for descriptions
#color_path: "#5cb2ff"       # htb blue code color for file paths
#color_border: "#1a2332"     # htb divider color
#color_cursor: "#ff8484"     # htb red for for cursor/selection indicator
#color_selected: "#1a2331"   # htb dark background for selected line
#color_dim: "#4a5466"        # htb border color for dimmed text

# Column widths (max characters)
column_header: 40
column_desc: 40
column_command: 60
column_gap: 4

# Only show cheats that have a <!-- cheat --> block
require_cheat_block: true

# Prompt for $var references in a command even when there's no matching
# <!-- cheat --> declaration. When false, undeclared variables are silently
# skipped.
allow_undeclared_vars: false

# Variable syntax accepted in cheat commands.
#   dollar (default) - only $name is a variable reference
#   angle            - only <name> is a variable reference
#   both             - accept both, resolving to the same variable
var_syntax: dollar

# Auto-continue when variables are prefilled from environment
# When true, if a var is already set (e.g., export domain=example.com),
# it will be used without prompting. Press ESC to go back and edit.
auto_continue: false

# Hooks (prepended/appended to the final command)
# Example: pre_hook: "cd ~/projects && "
# Example: post_hook: " && notify-send 'Done'"
pre_hook: ""
post_hook: ""
